<% provide(:title, @user.name) %>
<div class="row">
	<%= gravatar_for @user, :size => 200 %>
	<aside class="span4">
		<section>
			<h1>
				<%= @user.name %>
				<%= @user.email %>
			</h1>
		</section>
	</aside>
</div>
</br>
<%= button_to "Create board", new_board_path, :method => "get", class: "btn btn-large btn-inverse" %>